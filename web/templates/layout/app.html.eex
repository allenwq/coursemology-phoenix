<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Coursemology</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
  </head>

  <body>
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          <button aria-controls="navbar" aria-expanded="false" class="navbar-toggle collapsed" data-target="#site-navigation-navbar" data-toggle="collapse" type="button"><span class="sr-only">Toggle Navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button><a class="navbar-brand" href="/">Coursemology</a>
        </div>
        <div class="collapse navbar-collapse" id="site-navigation-navbar">
          <ul class="nav navbar-nav pull-right">
            <li>
              <a class="dropdown-toggle" data-toggle="dropdown">
                Courses
                <span class="caret"></span>
              </a>
              <%= if @current_user do %>
                <ul class="dropdown-menu pull-right">
                  <%= for course <- @current_user.courses do %>
                    <li>
                      <%= link course.title, to: course_path(@conn, :show, course) %>
                    </li>
                  <% end %>
                  <li>
                    <a href="/courses">All Courses</a>
                  </li>
                </ul>
              <% else %>
                  <a href="/courses">All Courses</a>
              <% end %>
            </li>
            <li>
              <a href="#">Help</a>
            </li>
            <li>
              <%= if @current_user do %>
                <a class="dropdown-toggle" data-toggle="dropdown">
                  <%= @current_user.name %>
                  <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                  <li>
                    <a href="/users/edit">Account Settings</a>
                  </li>
                  <li>
                    <a href="/admin">Administration Panel</a>
                  </li>
                  <li>
                    <a href="/admin/instance">Instance Administration Panel</a>
                  </li>
                  <li>
                    <a rel="nofollow" data-method="delete" href="/users/sign_out">Sign Out</a>
                  </li>
                </ul>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= if course_layout?(@conn) do %>
          <%= render "course.html", assigns %>
        <%= else %>
          <%= render @view_module, @view_template, assigns %>
        <% end %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
